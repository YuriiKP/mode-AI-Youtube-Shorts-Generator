# Local pipeline: download, transcription, highlight ranking, vertical crop.
yt-dlp>=2024.1.1
faster-whisper>=1.0.0
openai>=1.0.0
google-genai>=1.0.0
opencv-python==4.8
# torch is only needed for CUDA Whisper; CPU works without it.
# torch>=2.0

# Post-processing engine: burn subtitles in and mix background music.
moviepy==2.2.1
Pillow>=10.0
numpy>=1.25

# Scene/transition detection for the SLIDE_EFFECT pan (optional at runtime:
# scene_transitions.py falls back to OpenCV when this is not installed).
scenedetect>=0.6,<0.9
